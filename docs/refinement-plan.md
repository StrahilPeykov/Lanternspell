# Sleeping Orrery refinement — 2026-09-11

Start: `a14215b` on main, already containing the first playable and first benchmark. No branch integration is needed. Current runtime and original concept inspected. Preserve existing experimental models and signature alternatives.

1. Capture current Intel UHD High/Low baseline; isolate expensive renderer settings before choosing changes.
2. Repair stranded seeded hands with a visible free page turn; add a compact deterministic health/ward forecast so solo/duo opportunity costs are readable.
3. Refine facade inhabitation and central dial through the existing Blender script; soften paving and improve atmospheric background composition without a new region.
4. Repair reproduced client/seat lifecycle races and run scoped authority tests followed by real local solo/duo play, signatures, reconstruction and hardware sampling.
5. Record decisions, limitations and curated evidence, commit usable checkpoints directly to main and push.

Cloudflare production and non-production commands were changed to `npx wrangler deploy --dry-run` in the dashboard before any push. Build command remains `npm run build`. The previous public version stays unchanged; this run is local-only. Restore publishing only on explicit later authorization.

Lead owns render/UI/performance/integration/Git. Combat specialist owns pure rules and focused diagnostics. Art specialist owns benchmark-art.py/courtyard.glb. Network specialist owns client/authority lifecycle fixes and regression tests. No concurrent source edits during frame sampling; the first sampler was interrupted by HMR and discarded without performance claims.
