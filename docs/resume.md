# Resume / handoff

**Current refinement:** [ftue-results.md](ftue-results.md), [writing-style.md](writing-style.md), [traversal.md](traversal.md). Default Snappy/Gentle, hold Shift sprint; no jump system. Fresh profiles enter directly, learn practice progressively, choose tradition at the field book. `node tests/ftue-skimming.mjs` uses actual visible inputs and also works against production via GAME_URL. `node tests/traversal-compare.mjs` records the five movement/camera comparisons. Use ignored evidence/local for output. The older standalone signature comparison driver predates the title redesign; adapt its entry/navigation before reusing it, and do not treat historical reports as a current pass.


Root: `C:/Users/20211107/Desktop/Lanternspell`. Permanent branch **main**; commit verified checkpoints and immediately push origin/main. No branches, PRs, force pushes or paid services. **September 11 explicitly forbids publication.** Both Cloudflare production/version commands were saved as `npx wrangler deploy --dry-run`; leave them that way until new authorization. The September 10 public deployment/Free verification is historical. Current local refinement evidence: `docs/ftue-results.md`; earlier benchmark: `docs/refinement-results.md`. Two-device/country testing remains a human follow-up.

Launch: `npm ci`, `npm run dev`, http://127.0.0.1:5180. Do not stop unrelated projects. Production: `npm run build`, `npx vite preview --host 127.0.0.1 --port 5181`. The extra baseline comparison checkout under ignored tools/baseline is disposable diagnostic material, not a working branch.

Checks: `npm test` (68 passed), `npm run typecheck`, `npm run build`. Actual input: `node tests/benchmark-solo.mjs` (four tradition/model runs), `node tests/benchmark-duo.mjs`, signature treatments remain available in the development spellbook (older driver caveat above). Set `EVIDENCE_FILE` to an ignored local path to preserve historical reports. Real protocol: `node tests/network-real.mjs`; movement coalescing/correction: `node tests/network-movement-real.mjs`. Reconstruction commands remain in network.md. Production offline-after-load: `node tests/benchmark-production.mjs`. Finish builds, server startup and CPU tests before browser runs. `SCENARIO=mixed-book` or `same-hand` scopes duo; `RECORD_VIDEO=1` opts into its heavy dual recording, while screenshots/motion frames are always captured.

Normal new campaigns use full-book traditions; old data defaults baseline. Development comparison is in the spellbook. Seeded pages now turn the leftmost page on a free spell; paid spells replace their own slot. A complete legal joint plan previews end-of-round HP/ward using the pure resolver. Margin/Hearth decisions: combat-refinement.md and combat-benchmark.md. Shared config/facts/migration: benchmark-network.md; movement/rejoin fixes: network-review-2026-09-11.md. Authoring contracts: benchmark-art.md and art-refinement.md. Sound: audio.md. September 10 evidence remains in benchmark-results.md; current evidence is refinement-results.md.

Source boundaries: simulation/battle+traditions are pure; server owns durability/readiness. main is controller/dialogue glue; ui contains shell/combat markup; saves validates local import. render/environment builds repeated scenery, actors dresses the rig, effects owns disposable graphics, materials owns cached surfaces, world owns camera/render loop/load integration. No animation callback mutates gameplay.

Only curate milestone images and compact reports. New screenshots, videos and raw diagnostics go in evidence/local; no repeated .blend commits. Prettier is pinned project-locally for readable source. Old evidence and useful original .blend files remain unchanged in history.

Next human evaluation is the three focused comparisons in limitations.md. Do not add Chapter 2 to avoid resolving remaining pacing or visual weaknesses.

Performance: set `$env:PERF_OUTPUT='evidence/local/clean-performance.json'`, run `node scripts/benchmark-performance.mjs`, then clear that variable. Freeze runtime edits during sampling: HMR invalidates the measurement. Development-only `?renderProbe=no-aa` and `?renderProbe=no-shadows` isolate settings without changing production defaults. September 11 High median improved 50→33.4ms, but p95 remained 66.7ms; Low p95 improved 33.2→16.8ms on Intel UHD/D3D11. Preserve those limits and historical bad samples. No 1080p60 or broad weak-device acceptance claim.
